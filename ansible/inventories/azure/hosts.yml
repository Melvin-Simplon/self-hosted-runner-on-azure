# One host: the runner VM. Its IP comes from `terraform output public_ip`,
# passed through RUNNER_IP by the toolkit or the workflow.
runners:
  hosts:
    vm-runner:
      ansible_host: "{{ lookup('ansible.builtin.env', 'RUNNER_IP') }}"
